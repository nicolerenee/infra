---
# Enable NRI (Node Resource Interface) in containerd
# Required by DRANet for injecting network devices into pod containers
machine:
  files:
    - op: create
      path: /etc/cri/conf.d/20-nri.part
      content: |
        [plugins."io.containerd.nri.v1.nri"]
          disable = false
