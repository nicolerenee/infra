---
# yaml-language-server: $schema=https://k8s-schemas.freckle.systems/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wg-server-1-secrets
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    template:
      templateFrom:
        - configMap:
            name: wg-server-config-template
            items:
              - key: wg0.conf
  dataFrom:
    - extract:
        key: wg-server-1
---
# yaml-language-server: $schema=https://k8s-schemas.freckle.systems/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wg-server-2-secrets
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    template:
      templateFrom:
        - configMap:
            name: wg-server-config-template
            items:
              - key: wg1.conf
  dataFrom:
    - extract:
        key: wg-server-2
---
# yaml-language-server: $schema=https://k8s-schemas.freckle.systems/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wg-server-3-secrets
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    template:
      templateFrom:
        - configMap:
            name: wg-server-config-template
            items:
              - key: wg2.conf
  dataFrom:
    - extract:
        key: wg-server-3
