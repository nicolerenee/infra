---
# yaml-language-server: $schema=https://www.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./cnp.yaml
  - ./deployment.yaml
  - ./service.yaml
  - ./vmservicescrape.yaml
# Vendored froggeric chat template, content-hashed so edits roll the Deployment.
configMapGenerator:
  - name: qwen36-chat-template
    files:
      - chat_template.jinja
