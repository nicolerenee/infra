apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Install flux-system components first
  - flux-system
  # TODO: prometheus CRDs need to be installed first since cert manager uses them
  # TODO: install 1password and external secrets
  - cert-manager
